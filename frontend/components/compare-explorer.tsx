"use client";

import { ArrowPathIcon, ArrowsRightLeftIcon, LinkIcon } from "@heroicons/react/20/solid";
import { motion } from "motion/react";
import { useEffect, useState } from "react";

import { ComparisonActivityChart } from "@/components/charts";
import { ScoreBar, StatusBadge } from "@/components/ui";
import { API, type Activity, type Metric, type Repo } from "@/lib/api";
import { assessment, compact, duration, getNumber, percent } from "@/lib/format";

type ComparisonData = { metric: Metric; activity: Activity[] };

async function loadComparison(repository: string): Promise<ComparisonData> {
  const [owner, name] = repository.split("/");
  const [metricsResponse, activityResponse] = await Promise.all([fetch(`${API}/api/v1/repositories/${owner}/${name}/metrics`), fetch(`${API}/api/v1/repositories/${owner}/${name}/activity?weeks=12`)]);
  if (!metricsResponse.ok || !activityResponse.ok) throw new Error(`Could not load analytics for ${repository}`);
  return { metric: await metricsResponse.json(), activity: await activityResponse.json() };
}

export function CompareExplorer({ repositories, initialA, initialB }: { repositories: Repo[]; initialA?: string; initialB?: string }) {
  const valid = (value: string | undefined, fallback: string) => repositories.some((repo) => repo.full_name === value) ? value! : fallback;
  const [first, setFirst] = useState(valid(initialA, repositories[0]?.full_name ?? ""));
  const [second, setSecond] = useState(valid(initialB, repositories[1]?.full_name ?? ""));
  const [data, setData] = useState<Record<string, ComparisonData>>({});
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    if (!first || !second || first === second) return;
    let active = true; setLoading(true); setError("");
    window.history.replaceState(null, "", `/compare?a=${encodeURIComponent(first)}&b=${encodeURIComponent(second)}`);
    Promise.all([loadComparison(first), loadComparison(second)]).then(([left,right])=>active&&setData({[first]:left,[second]:right})).catch((reason:Error)=>active&&setError(reason.message)).finally(()=>active&&setLoading(false));
    return () => { active = false; };
  }, [first, second]);

  if (repositories.length < 2) return <div className="panel p-8 text-center text-sm text-[#9ba399]">Ingest at least two repositories to enable comparison.</div>;
  const left=data[first]?.metric; const right=data[second]?.metric;
  const copyLink=()=>navigator.clipboard.writeText(window.location.href);
  return <motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} className="space-y-6"><section className="panel p-5"><div className="grid items-end gap-3 md:grid-cols-[1fr_auto_1fr_auto]"><RepositorySelect label="Repository A" value={first} onChange={setFirst} repositories={repositories}/><button onClick={()=>{setFirst(second);setSecond(first)}} className="button-secondary !w-9 !px-0" title="Swap repositories"><ArrowsRightLeftIcon className="size-4"/></button><RepositorySelect label="Repository B" value={second} onChange={setSecond} repositories={repositories}/><button onClick={copyLink} className="button-secondary"><LinkIcon className="size-4"/>Copy link</button></div>{first===second&&<p className="mt-4 rounded-md bg-[#171b17] p-3 text-xs text-[#f1f4ec]">Choose two different repositories.</p>}{loading&&<p className="mt-4 inline-flex items-center gap-2 text-xs text-[#9ba399]"><ArrowPathIcon className="size-3.5 animate-spin"/>Loading equivalent-window evidence…</p>}{error&&<p className="mt-4 rounded-md bg-[#171b17] p-3 text-xs text-[#f1f4ec]">{error}</p>}</section>
    {left&&right&&first!==second&&<><section className="grid gap-4 lg:grid-cols-2"><Summary repository={first} metric={left} opponent={right}/><Summary repository={second} metric={right} opponent={left}/></section><section className="panel overflow-hidden"><div className="grid grid-cols-[1.3fr_1fr_1fr] border-b border-[#343a34] bg-[#080a08] px-5 py-4 text-xs text-[#f1f4ec]"><b>Measure</b><b>{first}</b><b>{second}</b></div><Group title="Strategic signals"/><ComparisonRow label="Momentum score" left={left.momentum_score} right={right.momentum_score} format="score" higher/><ComparisonRow label="Community health" left={left.health_score} right={right.health_score} format="score" higher/><ComparisonRow label="Contribution resilience" left={100-(left.bus_factor_risk??100)} right={100-(right.bus_factor_risk??100)} format="score" higher/><Group title="Delivery"/><ComparisonRow label="Human commits" left={getNumber(left,"velocity","commits")} right={getNumber(right,"velocity","commits")} higher/><ComparisonRow label="Commit change" left={getNumber(left,"velocity","commit_change")} right={getNumber(right,"velocity","commit_change")} format="percent" higher/><ComparisonRow label="Pull requests opened" left={getNumber(left,"velocity","pull_requests")} right={getNumber(right,"velocity","pull_requests")} higher/><ComparisonRow label="Pull requests merged" left={getNumber(left,"velocity","merged_pull_requests")} right={getNumber(right,"velocity","merged_pull_requests")} higher/><ComparisonRow label="Median PR merge cycle" left={getNumber(left,"responsiveness","median_pr_merge_hours")} right={getNumber(right,"responsiveness","median_pr_merge_hours")} format="duration"/><ComparisonRow label="Issue net flow (closed − opened)" left={getNumber(left,"velocity","net_issue_flow")} right={getNumber(right,"velocity","net_issue_flow")} higher/><Group title="Community & evidence"/><ComparisonRow label="Active human contributors" left={getNumber(left,"community","active_contributors")} right={getNumber(right,"community","active_contributors")} higher/><ComparisonRow label="Effective contributors (1 / HHI)" left={getNumber(left,"concentration","effective_contributors")} right={getNumber(right,"concentration","effective_contributors")} higher/><ComparisonRow label="Top contributor share" left={getNumber(left,"concentration","top_1_share")} right={getNumber(right,"concentration","top_1_share")} format="percent"/><ComparisonRow label="Automation share" left={getNumber(left,"velocity","automation_share")} right={getNumber(right,"velocity","automation_share")} format="percent"/><ComparisonRow label="Evidence confidence" left={getNumber(left,"data_quality","confidence_score")} right={getNumber(right,"data_quality","confidence_score")} format="score" higher/></section><section className="panel p-5"><h2 className="section-title">Human commit activity</h2><p className="mb-5 mt-1 text-xs text-[#9ba399]">Aligned by UTC date across the last 12 weekly buckets.</p><ComparisonActivityChart leftName={first} rightName={second} left={data[first].activity} right={data[second].activity}/></section></>}
  </motion.div>;
}

function RepositorySelect({label,value,onChange,repositories}:{label:string;value:string;onChange:(value:string)=>void;repositories:Repo[]}){return <label className="data-label">{label}<select value={value} onChange={(event)=>onChange(event.target.value)} className="mt-2 h-10 w-full rounded-md border border-[#343a34] bg-[#101310] px-3 font-mono text-xs text-[#f1f4ec]">{repositories.map((repo)=><option key={repo.full_name}>{repo.full_name}</option>)}</select></label>}
function Summary({repository,metric,opponent}:{repository:string;metric:Metric;opponent:Metric}){const state=assessment(metric);const advantages=[{label:"Momentum",value:metric.momentum_score??0,other:opponent.momentum_score??0},{label:"Health",value:metric.health_score??0,other:opponent.health_score??0},{label:"Resilience",value:100-(metric.bus_factor_risk??100),other:100-(opponent.bus_factor_risk??100)}].filter((item)=>item.value-item.other>=5).map((item)=>item.label);return <article className="panel p-5"><div className="flex items-center justify-between gap-3"><h2 className="font-mono text-sm font-semibold">{repository}</h2><StatusBadge status={state.status} tone={state.tone}/></div><div className="mt-5 grid grid-cols-3 gap-4"><MiniScore label="Momentum" value={metric.momentum_score}/><MiniScore label="Health" value={metric.health_score}/><MiniScore label="Resilience" value={100-(metric.bus_factor_risk??100)}/></div><p className="mt-5 border-t border-[#343a34] pt-4 text-xs leading-5 text-[#9ba399]">{advantages.length?`Material advantage in ${advantages.join(" and ").toLowerCase()}.`:"No material composite-score advantage; inspect the operating measures below."}</p></article>}
function MiniScore({label,value}:{label:string;value:number|null}){return <div><p className="text-[10px] text-[#9ba399]">{label}</p><div className="mt-2"><ScoreBar compact value={value}/></div></div>}
function Group({title}:{title:string}){return <div className="border-b border-[#343a34] bg-[#171b17] px-5 py-2.5 text-[10px] font-bold uppercase tracking-[.1em] text-[#9ba399]">{title}</div>}
function ComparisonRow({label,left,right,format="number",higher=false}:{label:string;left:number|null;right:number|null;format?:"number"|"score"|"percent"|"duration";higher?:boolean}){const difference=left!=null&&right!=null?Math.abs(left-right):0;const material=format==="percent"?difference>=.05:difference>=5;const leftWins=material&&left!=null&&right!=null&&(higher?left>right:left<right);const rightWins=material&&left!=null&&right!=null&&(higher?right>left:right<left);const display=(value:number|null)=>format==="percent"?percent(value,true):format==="duration"?duration(value):format==="score"?(value==null?"—":Math.round(value).toString()):compact(value);return <div className="grid grid-cols-[1.3fr_1fr_1fr] items-center border-b border-[#343a34] px-5 py-3.5 text-xs last:border-0"><span className="text-[#9ba399]">{label}</span><b className={`font-mono ${leftWins?"text-[#c7ff00]":""}`}>{display(left)}</b><b className={`font-mono ${rightWins?"text-[#c7ff00]":""}`}>{display(right)}</b></div>}
